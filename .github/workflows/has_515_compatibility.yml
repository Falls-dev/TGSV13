# Legacy BeeStation 515 merge-skew marker. Retained as a no-op so old PR checks
# do not hard-fail while the repo targets BYOND 516.
name: "516 Compatibility Pass"
on:
  pull_request:
    branches:
    - master
jobs:
  has_516_compatibility:
    name: Has 516 Compatibility
    runs-on: ubuntu-22.04
    steps:
      - run: echo "You're ready to go!"
